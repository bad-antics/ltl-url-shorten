// examples/demo.ltl — runnable demo for ltl_url_shorten

import std::io
use ltl_url_shorten as lib

fn main() {
    io::println("ltl_url_shorten demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}